/* let posinsert = document.getElementById("posInsert");
let posremove = document.getElementById("posRemove");

while (true) {
    posinsert.setAttribute("max", nueva.length);
    posremove.setAttribute("max", nueva.length);
} */